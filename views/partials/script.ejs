<!-- Google jQuery -->
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script>
$(document).ready(function() {
    $("#personalInfoButton").click(function(event) {
        event.preventDefault();
        $("#personalInfoButton").css("background-color", "red");
        
        var data = {
            event: "Button: Kişisel Bilgi Formu düğmesine basıldı."
        };
        $.ajax({
            type: "POST",
            url: "/log",
            data: data,
            success: function(response) {
                console.log("Kişisel Bilgi Formu");
            }
        });
    });
    
    $("#startExamModalButton").click(function(event) {
        event.preventDefault();
        $("#startExamModalButton").css("background-color", "red");
        
        var data = {
            event: "Button: Sınava Başla düğmesine basıldı."
        };
        $.ajax({
            type: "POST",
            url: "/log",
            data: data,
            success: function(response) {
                    console.log("Kişisel Bilgi Formu");
            }
        });
    });
    
});

$(document).ready(function() {
    $("#greetingMsg").delay(5000).slideUp(400);
    $("#msgPersonalInfo").delay(5000).slideUp(400);
});

$(document).ready(function() {
    /* Kişisel Bilgi Formu */
    /* ana sayfadaki yönlendirme düğmesi */
    $("#personalInfoButton").click((event) => {
        event.preventDefault();
        window.location.href = "/user/personal-info";
    });
    
    /* gender */
    $("#alertGenderDiv").hide();
    
    /* job */
    $("#alertJobDiv").hide();
    $("#jobStudentInfo").hide();
    $("#jobOtherInfo").hide();
    /* .toggle() */
    /*$("#inputCheckJob1").click(function() {
        $("#jobStudentInfo").toggle(this.checked);
    });
    $("#inputCheckJob2").click(function() {
        $("#jobOtherInfo").toggle(this.checked);
    });*/
    
    $("#inputCheckJob1").change(function() {
        if($(this).is(':checked')) {
            $("#jobStudentInfo").show(300).find(':input').attr('required', true);
        } else {
            $("#jobStudentInfo").hide(300).find(':input').removeAttr('required');
        }
    });
    $("#inputCheckJob2").change(function() {
        if($(this).is(':checked')) {
            $("#jobOtherInfo").show(300).find(':input').attr('required', true);
        } else {
            $("#jobOtherInfo").hide(300).find(':input').removeAttr('required');
        }
    });
    
    /* personal techs */
    $("#personalTechsOtherInfo").hide();
    $("#personalTechCheck6").change(function() {
        if($(this).is(':checked')) {
            $("#personalTechsOtherInfo").show(300).find(':input').attr('required', true);
        } else {
            $("#personalTechsOtherInfo").hide(300).find(':input').removeAttr('required');
        }
    });
    
    /* operation system */
    $("#opSystemOtherInfo").hide();
    $("#opSystemCheck3").change(function() {
        if($(this).is(':checked')) {
            $("#opSystemOtherInfo").show(300).find(':input').attr('required', true);
        } else {
            $("#opSystemOtherInfo").hide(300).find(':input').removeAttr('required');
        }
    });
    
    /* personal info alerts */
    $("#personalInfoSaveButton").click(function(e) {
        /* gender */
        if($("input[name='gender']").val() === null) {
          
            e.preventDefault();
            $("#alertGenderDiv").show(300);
            $("#alertGenderDiv").attr("tabindex","-1");
            document.getElementById("alertGenderDiv").focus();
            $(this).css("background-color", "red");
            $("#alertGenderDiv").delay(5000).slideUp(400);
        }
        
        /* job */
        if(($("input[name='studentVar1']").val() === "") && ($("input[name='studentVar2']").val() === "") && ($("input[name='job']").val() === "")) {
            
            e.preventDefault();
            $("#alertJobDiv").show(300);
            $("#alertJobDiv").attr("tabindex","0");
            document.getElementById("alertJobDiv").focus();
            $(this).css("background-color", "red");
            $("#alertJobDiv").delay(5000).slideUp(400);
        }
    });
    

});

$(function() {
    
});
</script>

<!--
/* personal info form alerts */
$("#personalInfoSaveButton").click(function(e) {
    $("#inputCheckJob1, #inputCheckJob2").each(function() {
        if(($("#inputCheckJob1").prop("checked", false)) && ($("#inputCheckJob2").prop("checked", false))) {
            
            
            $("#alertJobDiv").show(300);
            $("#personalInfoSaveButton").css("background-color", "red");
        } else {
            $("#personalInfo").submit();
        }
        
    });
    
});

$(function() {
    /* personal info form alerts */
    $("#personalInfo").each(function() {
    $(this).submit(function(event) {
        /*if ($.trim(username.val()) === "")
        !$("#inputCheckJob1").is(':checked') && !$("#inputCheckJob2").is(':checked'))*/
        event.preventDefault();
        alert("it is working");
        if(($("#inputCheckJob1").prop("checked", false)) && ($("#inputCheckJob2").prop("checked", false))) {
            
            $("#alertJobDiv").show(300);
            $("#personalInfoSaveButton").css("background-color", "red");
        }
        return false;
    });
    });
});
-->
